Read the entire project and continue from where the previous agent stopped.

The previous completed task was rebuilding the 5-scene campaign master (DreamPlanet_Master_v2_audio.mp4). The next task is Plan #3: Confirm the campaign plays smoothly end-to-end after Scene 5 is added.

Objective

Perform a comprehensive human-style QA pass on the entire 5-scene commercial (approximately 41.6 seconds). Do not modify any assets unless a defect is found.

Review checklist

Watch the complete master from beginning to end multiple times, including frame-by-frame review at every scene boundary.

Verify:

* Scene 1 → Scene 2
    * smooth pacing
    * no flash
    * no duplicate frames
    * audio remains continuous
* Scene 2 → Scene 3
    * no white-screen artifact
    * dissolve looks natural
    * Scene 3 begins immediately after transition
    * timing feels intentional
* Scene 3
    * slower timing is preserved
    * Referral Home
    * View Levels
    * Leaderboard
    * all tap animations complete
    * users have enough time to read each page
    * no skipped animations
* Scene 3 → Scene 4
    * leaderboard continuity is perfect
    * no camera jump
    * no visual mismatch
    * audio uninterrupted
* Scene 4
    * verify every animation still matches the updated Scene 3 ending
    * verify timing remains correct
* Scene 4 → Scene 5
    * transition feels intentional
    * no frame duplication
    * no flicker
    * no visible cut artifact
* Scene 5
    * portfolio page no longer shows forum FAB incorrectly
    * forum page does NOT display the yellow add-post button
    * forum scrolls through multiple posts
    * scrolling speed feels comfortable
    * likes/comments/share animations complete
    * portfolio timing feels readable
    * forum timing feels readable
    * ending frame is stable

Audio QA

Verify:

* continuous music
* no restart
* no silence
* no clicks
* no pops
* fadeout only occurs at the very end

Technical QA

Confirm:

* 1080×1920
* expected frame rate
* expected duration
* audio/video sync maintained
* no dropped frames
* no black frames
* no white frames
* no unexpected blank sections

Deliverables

Produce:

* MASTER_V2_QA_REPORT.md

Include:

* pass/fail for every scene
* pass/fail for every transition
* pass/fail for audio
* pass/fail for pacing
* any issues discovered
* screenshots if defects are found
* precise timestamps for every issue

If no defects are found, explicitly certify:

DreamPlanet_Master_v2_audio.mp4 is approved as the canonical 5-scene master and is ready to serve as the baseline before beginning Scene 6.

Do not modify any files unless an actual defect is discovered. This is a QA-only task.